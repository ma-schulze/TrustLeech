# Claim 2 -Transparency

These testcases evaluate the transparency claims made in Section 5.2 of the paper. 

To reproduce these tests, execute the `run.sh` file accompanying this claim. 
This opens a new QEMU VM, using the images generated by the install script. 
Log into the VM using the username `root`.

To verify the claims, execute the following steps:


## Memory Footprint

To evaluate the memory footprint of the TrustLeech-hypervisor, we evaluate the size of its binary that is added to the system:
```
ls -lh artifact/buildroot/output/build/trustleech-custom/src/trustleech   
```

